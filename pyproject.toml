[project]
name = "scs-blog-worker"
version = "1.0.0"
description = "SCS Blog Telegram Worker"
requires-python = ">=3.13"

dependencies = [
    "beautifulsoup4",
    "httpx",
]

[dependency-groups]
dev = [
    "workers-py",
    "workers-runtime-sdk",
]
